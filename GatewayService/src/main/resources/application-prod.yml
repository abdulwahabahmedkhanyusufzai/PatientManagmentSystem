spring:
  main:
    lazy-initialization: true

management:
  endpoint:
    health:
      probes:
        enabled: true
  tracing:
    sampling:
      probability: 0.1

logging:
  level:
    root: WARN

server:
  shutdown: graceful
